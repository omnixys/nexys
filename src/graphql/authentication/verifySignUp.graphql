 mutation VerifySignUp($token: String!) {
   verifySignUp(token: $token) {
     token {
       accessToken
       expiresIn
       refreshToken
       refreshExpiresIn
       idToken
       scope
     }
     message
     username
   }
 }
